<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{pageTitle}}</title>

    <style>
        :root {
            --bg-color: #f4f7fa;
            --card-bg: #ffffff;
            --text-color: #333;
            --text-muted: #777;
            --border-color: #e0e6ed;
            --accent-blue: #3b82f6;
            --available-color: #66cc99;
            --available-bg: #f0f9f5;
            --occupied-color: #ff9966;
            --occupied-bg: #fff8f0;
        }
        * { box-sizing: border-box; }
        html, body {
            height: 100vh;
            margin: 0;
            padding: 0;
            overflow: hidden;
        }
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
            background-color: var(--bg-color);
            color: var(--text-color);
            display: flex;
            flex-direction: column;
        }
        .dashboard-container {
            max-width: 100%;
            height: 100vh;
            display: flex;
            flex-direction: column;
            padding: 16px;
            gap: 12px;
        }
        .dashboard-header {
            flex-shrink: 0;
        }
        .dashboard-header h1 {
            margin: 0;
            font-size: 1.5rem;
            font-weight: 600;
        }
        .dashboard-header p {
            margin: 0;
            font-size: 0.875rem;
            color: var(--text-muted);
        }
        .stats-summary {
            display: flex;
            gap: 12px;
            flex-shrink: 0;
        }
        .stat-card {
            display: flex;
            justify-content: space-between;
            align-items: center;
            background: var(--card-bg);
            border: 1px solid var(--border-color);
            border-radius: 8px;
            padding: 16px;
            flex: 1;
        }
        .stat-card .stat-info {
            display: flex;
            flex-direction: column;
            gap: 8px;
        }
        .stat-card .stat-info .stat-label {
            margin: 0;
            color: var(--text-color);
            font-size: 0.9rem;
            font-weight: 500;
        }
        .stat-card .stat-info .stat-value-row {
            display: flex;
            align-items: baseline;
            gap: 8px;
        }
        .stat-card .stat-info h2 {
            margin: 0;
            font-size: 2.5rem;
            font-weight: 700;
            color: var(--available-color);
        }
        .stat-card .stat-info .stat-suffix {
            margin: 0;
            color: var(--available-color);
            font-size: 1rem;
            font-weight: 600;
        }
        .stat-card .stat-icon-wrapper {
            font-size: 1.5rem;
            padding: 8px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .stat-icon-wrapper.car-icon {
            color: var(--accent-blue);
            background-color: #e0eafc;
        }
        .stat-icon-wrapper.motorcycle-icon {
            color: #f59e0b;
            background-color: #fef9c3;
        }
        .main-content {
            display: flex;
            gap: 16px;
            flex: 1;
            min-height: 0;
            overflow: hidden;
        }
        .parking-layout {
            flex: 3;
            background: var(--card-bg);
            border: 1px solid var(--border-color);
            border-radius: 8px;
            padding: 16px;
            overflow-y: auto;
            display: flex;
            flex-direction: column;
            gap: 16px;
        }
        .floor-section {
            flex-shrink: 0;
        }
        .floor-section h2 {
            font-size: 1rem;
            font-weight: 600;
            margin: 0 0 12px 0;
            border-bottom: 1px solid var(--border-color);
            padding-bottom: 6px;
        }
        .floor-grid {
            display: grid;
            grid-template-columns: repeat(5, 1fr) 40px repeat(5, 1fr) 40px repeat(5, 1fr);
            gap: 6px;
        }
        .spot {
            border: 2px solid var(--border-color);
            border-radius: 4px;
            padding: 4px;
            text-align: center;
            font-size: 0.7rem;
            font-weight: 500;
            min-height: 40px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            position: relative;
        }
        .spot-icon {
            font-size: 1rem;
            line-height: 1;
        }
        .spot-id {
            color: var(--text-muted);
            font-weight: 600;
            font-size: 0.65rem;
        }
        .spot.available {
            border-color: var(--available-color);
            background-color: var(--available-bg);
        }
        .spot.occupied {
            border-color: var(--occupied-color);
            background-color: var(--occupied-bg);
        }
        .aisle-label {
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            font-size: 0.65rem;
            color: var(--text-muted);
            font-weight: 500;
            writing-mode: vertical-rl;
            transform: rotate(180deg);
        }
        .street-separator {
            grid-column: 1 / -1;
            text-align: center;
            color: var(--text-muted);
            font-weight: 500;
            font-size: 0.75rem;
            padding: 4px 0;
            border-top: 1px dashed var(--border-color);
            border-bottom: 1px dashed var(--border-color);
            margin: 2px 0;
        }
        .empty-cell {
            visibility: hidden;
        }
        .sidebar {
            flex: 1;
            max-width: 260px;
            display: flex;
            flex-direction: column;
            gap: 12px;
            overflow-y: auto;
        }
        .legend-card {
            background: var(--card-bg);
            border: 1px solid var(--border-color);
            border-radius: 8px;
            padding: 16px;
            flex-shrink: 0;
        }
        .legend-card h3 {
            font-size: 1rem;
            font-weight: 600;
            margin: 0 0 12px 0;
            color: var(--text-color);
        }
        .legend-item {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 10px;
            font-size: 0.85rem;
            padding: 8px;
            background: var(--bg-color);
            border-radius: 6px;
        }
        .legend-item:last-child {
            margin-bottom: 0;
        }
        .legend-item-content {
            display: flex;
            align-items: center;
            gap: 10px;
        }
        .legend-box {
            width: 20px;
            height: 20px;
            border-radius: 4px;
            flex-shrink: 0;
        }
        .legend-box.available {
            border: 2px solid;
            border-color: var(--available-color);
            background-color: var(--available-bg);
        }
        .legend-box.occupied {
            border: 2px solid;
            border-color: var(--occupied-color);
            background-color: var(--occupied-bg);
        }
        .legend-text {
            font-weight: 500;
            color: var(--text-color);
        }
        .legend-text.available-text {
            color: var(--available-color);
        }
        .legend-text.occupied-text {
            color: var(--occupied-color);
        }
        .legend-icon {
            font-size: 1.2rem;
            flex-shrink: 0;
        }
        .fee-item {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 10px;
            padding: 12px;
            background: var(--bg-color);
            border-radius: 6px;
            border-left: 3px solid var(--accent-blue);
        }
        .fee-item:last-child {
            margin-bottom: 0;
        }
        .fee-item-left {
            display: flex;
            align-items: center;
            gap: 10px;
        }
        .fee-vehicle-type {
            font-size: 0.85rem;
            font-weight: 500;
            color: var(--text-color);
        }
        .fee-amount {
            font-size: 1.1rem;
            font-weight: 700;
            color: var(--accent-blue);
        }
        .fee-currency {
            font-size: 0.75rem;
            font-weight: 600;
            color: var(--text-muted);
            margin-left: 2px;
        }
    </style>
</head>
<meta http-equiv="refresh" content="60">
<body>

<div class="dashboard-container">
    {{{body}}}
</div>

</body>
</html>